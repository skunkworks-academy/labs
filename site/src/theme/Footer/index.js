// The Academy-wide footer is injected dynamically by
// https://www.skunkworksacademy.com/assets/academy-navigation.js.
// Keeping the Docusaurus footer disabled prevents duplicate global chrome.
export default function Footer() {
  return null;
}

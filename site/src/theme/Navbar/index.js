// The Academy-wide navigation is injected dynamically by
// https://www.skunkworksacademy.com/assets/academy-navigation.js.
// Keeping the Docusaurus navbar disabled prevents duplicate navigation and FOUC.
export default function Navbar() {
  return null;
}
